<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Skilled.ViewModels"
             x:Class="Skilled.Views.LoginPage"
             Title="Login"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ContentPage.BindingContext>
        <viewmodels:LoginPageViewModel />
    </ContentPage.BindingContext>
    
    <Grid RowDefinitions="Auto,*,Auto" Padding="20">
        <!-- Logo and App Title -->
        <StackLayout Grid.Row="0" Spacing="20" Margin="0,60,0,0">
            <Image Source="logo.png" HeightRequest="80" WidthRequest="80" HorizontalOptions="Center" />
            <Label Text="Welcome to Skilled" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Connect with skilled professionals" FontSize="16" TextColor="{DynamicResource Gray500}" HorizontalOptions="Center" />
        </StackLayout>
        
        <!-- Login Form -->
        <VerticalStackLayout Grid.Row="1" Spacing="20" VerticalOptions="Center">
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />
            
            <Button Text="Log In" Command="{Binding LoginCommand}" 
                    BackgroundColor="{DynamicResource Primary}" TextColor="White"
                    FontAttributes="Bold" CornerRadius="10" HeightRequest="50"
                    IsEnabled="{Binding IsNotBusy}" />
                    
            <Button Text="Forgot Password?" Command="{Binding ForgotPasswordCommand}" 
                    BackgroundColor="Transparent" TextColor="{DynamicResource Primary}"
                    FontSize="14" />
                    
            <Label Text="{Binding ErrorMessage}" TextColor="Red" 
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                   HorizontalOptions="Center" />
                   
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                               HorizontalOptions="Center" VerticalOptions="Center" />
        </VerticalStackLayout>
        
        <!-- Register Prompt -->
        <HorizontalStackLayout Grid.Row="2" Spacing="5" HorizontalOptions="Center">
            <Label Text="Don't have an account?" VerticalOptions="Center" />
            <Button Text="Sign Up" Command="{Binding RegisterCommand}" 
                    BackgroundColor="Transparent" TextColor="{DynamicResource Primary}"
                    FontAttributes="Bold" Padding="0" />
        </HorizontalStackLayout>
    </Grid>

</ContentPage>