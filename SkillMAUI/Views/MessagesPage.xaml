<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skilled.Views.MessagesPage"
             Title="Messages">

    <Grid RowDefinitions="*,Auto">

        <!-- Chat Previews List -->
        <CollectionView Grid.Row="0"
                       ItemsSource="{Binding ChatPreviews}"
                       SelectionMode="Single"
                       SelectionChanged="OnChatSelected">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10">
                    <Label Text="No messages yet" FontSize="18" TextColor="Gray" HorizontalOptions="Center" />
                    <Label Text="Book a service to start chatting" FontSize="14" TextColor="LightGray" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="1" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="15" BackgroundColor="White" Margin="0,0,0,1">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <Image Grid.Column="0" Source="{Binding ProfileImage}"
                                   HeightRequest="50" WidthRequest="50"
                                   Aspect="AspectFill" />

                            <VerticalStackLayout Grid.Column="1" Spacing="4" Margin="15,0">
                                <Label Text="{Binding Name}" FontAttributes="Bold" />
                                <Label Text="{Binding LastMessage}" FontSize="12" TextColor="Gray"
                                       MaxLines="1" LineBreakMode="TailTruncation" />
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="2" HorizontalOptions="End" Spacing="5">
                                <Label Text="{Binding Timestamp, StringFormat='{0:HH:mm}'}"
                                       FontSize="10" TextColor="Gray" />
                                <Frame BackgroundColor="Red" CornerRadius="10"
                                       IsVisible="{Binding HasUnreadMessages}"
                                       WidthRequest="20" HeightRequest="20" Padding="0">
                                    <Label Text="{Binding UnreadCount}" FontSize="10"
                                           TextColor="White" HorizontalOptions="Center"
                                           VerticalOptions="Center" />
                                </Frame>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="0"
                          IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          HorizontalOptions="Center"
                          VerticalOptions="Center" />

    </Grid>

</ContentPage>
