<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Skilled.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Skilled"
    xmlns:views="clr-namespace:Skilled.Views"
    Shell.FlyoutBehavior="Disabled">

    <!-- Login and Registration Flow -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage" />
        
    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Route="RegisterPage" />

    <!-- Main Application Tabs -->
    <TabBar x:Name="MainTabs" Route="MainTabs">
        <Tab Title="Home" Icon="home.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate views:HomePage}"
                Route="HomePage" />
        </Tab>
        
        <Tab Title="Messages" Icon="message.png">
            <ShellContent
                Title="Messages"
                ContentTemplate="{DataTemplate views:MessagesPage}"
                Route="MessagesPage" />
        </Tab>
        
        <Tab Title="Bookings" Icon="calendar.png">
            <ShellContent
                Title="Bookings"
                ContentTemplate="{DataTemplate views:BookingsPage}"
                Route="BookingsPage" />
        </Tab>
        
        <Tab Title="Settings" Icon="settings.png">
            <ShellContent
                Title="Settings"
                ContentTemplate="{DataTemplate views:SettingsPage}"
                Route="SettingsPage" />
        </Tab>
    </TabBar>

</Shell>
